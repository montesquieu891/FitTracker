name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.12"

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Ruff lint
        run: python -m ruff check src/ tests/

      - name: Ruff format check
        run: python -m ruff format --check src/ tests/

      - name: Mypy type check
        run: python -m mypy src/fittrack/ --ignore-missing-imports

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Run unit tests with coverage
        env:
          APP_ENV: testing
        run: |
          python -m pytest tests/unit/ \
            --cov=src/fittrack \
            --cov-report=term-missing \
            --cov-report=xml \
            -v

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage.xml

  # Integration tests require Oracle container — run on self-hosted or manually
  # integration:
  #   name: Integration Tests
  #   runs-on: ubuntu-latest
  #   needs: test
  #   services:
  #     oracle:
  #       image: container-registry.oracle.com/database/free:latest
  #       env:
  #         ORACLE_PWD: oracle_ci_pwd
  #       ports:
  #         - 1521:1521
  #       options: >-
  #         --health-cmd "echo 'SELECT 1 FROM DUAL;' | sqlplus -s sys/oracle_ci_pwd@localhost:1521/FREEPDB1 as sysdba"
  #         --health-interval 30s
  #         --health-timeout 10s
  #         --health-retries 10
  #         --health-start-period 120s
  #     redis:
  #       image: redis:7-alpine
  #       ports:
  #         - 6379:6379
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: ${{ env.PYTHON_VERSION }}
  #     - run: pip install -e ".[dev]"
  #     - name: Run integration tests
  #       env:
  #         ORACLE_DSN: localhost:1521/FREEPDB1
  #         ORACLE_USER: fittrack
  #         ORACLE_PASSWORD: fittrack_ci
  #         REDIS_URL: redis://localhost:6379/0
  #       run: python -m pytest tests/integration/ -v -m integration
